const form=document.getElementById("form"),file=document.getElementById("resume"),nameEl=document.getElementById("filename"),results=document.getElementById("results"),error=document.getElementById("error"),btn=document.getElementById("btn");
file.addEventListener("change",function(){nameEl.textContent=file.files[0]?file.files[0].name:"Maximum 5 MB"});
form.addEventListener("submit",async function(e){e.preventDefault();error.textContent="";btn.disabled=true;btn.textContent="Analyzing...";
try{const res=await fetch("/analyze",{method:"POST",body:new FormData(form)}),data=await res.json();if(!res.ok)throw new Error(data.error||"Analysis failed");render(data)}catch(err){error.textContent=err.message;results.classList.add("hidden")}finally{btn.disabled=false;btn.textContent="Analyze My Resume →"}});
function chips(id,items){const el=document.getElementById(id);el.innerHTML=items.length?items.map(function(x){return '<span class="chip">'+x+"</span>"}).join(""):'<span class="chip">None detected</span>'}
function render(d){document.getElementById("score").textContent=d.score+"%";document.getElementById("ring").style.setProperty("--score",d.score+"%");document.getElementById("summary").textContent=d.score>=75?"Strong alignment with detected requirements.":d.score>=50?"Moderate alignment — review the missing skills.":"Several job requirements are not detected in the resume.";chips("matched",d.matched);chips("missing",d.missing);document.getElementById("suggestions").innerHTML=d.suggestions.map(function(x){return "<li>"+x+"</li>"}).join("");results.classList.remove("hidden");results.scrollIntoView({behavior:"smooth"})}